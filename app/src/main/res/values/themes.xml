<resources>
    <!-- Base application theme -->
    <style name="Theme.PocketMoney" parent="android:Theme.Material.Light.NoActionBar" />
    <style name="Theme.PocketMoney.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.PocketMoney</item>
    </style>
</resources>
